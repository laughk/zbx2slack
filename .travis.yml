language: python
python:
  - '2.6'
  - '2.7'
  - '3.3'
  - '3.4'
  - '3.5'
install:
  # for python2.6
  - if [[ $(python -V | awk '{print $NF}')$ =~ ^2.6 ]]; then pip install argparse; fi

script: nosetest -v
